apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vehicle
  namespace: vehicle
spec:
  hosts:
  - vehicle-api-svc.vehicle.svc.cluster.local

  http:
  - route:
    - destination:
        host: vehicle-api-svc
        port:
          number: 8080
                 